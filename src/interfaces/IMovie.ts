export interface IMovie {
    id: number,
    title: string,
    year: number,
    rating: string,
    runtime: number,
    averageRating: number,
}

export interface IMovieDetails {
    id: number,
    title: string,
    description: string,
    year: number,
    rating: string,
    runtime: number,
    averageRating: number,
}